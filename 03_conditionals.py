# Python Conditional Statements

# [if✅, elif✅, else✅, comparison operators ✅, logical operators — how to make decisions in code]

# Exercise:
# 1. Write a program that checks a user's age and prints whether they are a "Minor", "Adult", or "Senior" (65+).

# 2. Simulate a login check — given an email and password variable, validate that neither is empty, the email contains "@", and the password is at least 8 characters. Print the appropriate message for each failure case and "Login successful" if everything passes.

# 3. Given a role variable, print what the user can do — admins can do everything, editors can create and edit, viewers can only read, and anything else should be denied.

# 4. Given a cart_total variable, apply a discount: 20% off if total is above 500, 10% off if above 200, no discount otherwise. Print the final amount.
